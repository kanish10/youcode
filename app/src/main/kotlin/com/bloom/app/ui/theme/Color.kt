package com.bloom.app.ui.theme

import androidx.compose.ui.graphics.Color

object BloomColors {
    // --- Quadrant accent colors (used in WellnessQuadrant model, sub-screens) ---
    val mindBlue = Color(0xFF7EB8E0)
    val mindBlueBg = Color(0xFFE8F2FA)
    val bodyGreen = Color(0xFF8BC6A3)
    val bodyGreenBg = Color(0xFFE8F5EE)
    val soulPink = Color(0xFFE4A0B7)
    val soulPinkBg = Color(0xFFFBEAF0)
    val connectAmber = Color(0xFFE8C170)
    val connectAmberBg = Color(0xFFFAEEDA)

    // --- Legacy text aliases (used by existing screens) ---
    val textPrimary = Color(0xFF383833)
    val textSecondary = Color(0xFF65655E)
    val textTertiary = Color(0xFF9C9A92)

    // Garden visualization
    val gardenGreen = Color(0xFFC8E6C9)
    val soil = Color(0xFF8D6E63)

    // --- Design system (from Stitch/Haven reference) ---

    // Brand colors (top-level identity)
    val sageGreen = Color(0xFF8FA89B)
    val warmBeige = Color(0xFFA89B8F)
    val softLavender = Color(0xFF9B8FA8)

    // Primary (sage green tones)
    val primary = Color(0xFF52695E)
    val primaryContainer = Color(0xFFCEE9DA)
    val onPrimary = Color(0xFFFFFFFF)
    val onPrimaryContainer = Color(0xFF40574C)

    // Secondary (warm beige tones)
    val secondary = Color(0xFF6D6258)
    val secondaryContainer = Color(0xFFEFE0D2)
    val onSecondary = Color(0xFFFFFFFF)
    val onSecondaryContainer = Color(0xFF5A5046)

    // Tertiary (lavender tones)
    val tertiary = Color(0xFF6B6077)
    val tertiaryContainer = Color(0xFFEEE0FC)
    val onTertiary = Color(0xFFFFFFFF)
    val onTertiaryContainer = Color(0xFF594F66)

    // Surfaces
    val background = Color(0xFFFFFCF7)
    val surface = Color(0xFFFFFFFF)
    val creamBg = Color(0xFFF5F2ED)
    val surfaceContainer = Color(0xFFF6F3EC)
    val surfaceContainerLow = Color(0xFFFCF9F3)
    val surfaceContainerHigh = Color(0xFFF0EEE6)

    // On-surface text
    val onSurface = Color(0xFF383833)
    val onSurfaceVariant = Color(0xFF65655E)

    // Outlines / borders
    val outline = Color(0xFF81817A)
    val outlineVariant = Color(0xFFBBB9B2)

    // Bottom nav inactive state
    val inactive = Color(0xFFA8A29E)
}
